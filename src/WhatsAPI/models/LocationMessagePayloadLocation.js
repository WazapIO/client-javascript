/**
 * WhatsAPI Go
 * The V2 of WhatsAPI Go
 *
 * The version of the OpenAPI document: 2.0
 * Contact: manjit@sponsorbook.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The LocationMessagePayloadLocation model module.
 * @module WhatsAPI/models/LocationMessagePayloadLocation
 * @version 1.0.0
 */
class LocationMessagePayloadLocation {
    /**
     * Constructs a new <code>LocationMessagePayloadLocation</code>.
     * @alias module:WhatsAPI/models/LocationMessagePayloadLocation
     * @param latitude {Number} 
     * @param longitude {Number} 
     * @param name {String} 
     */
    constructor(latitude, longitude, name) { 
        
        LocationMessagePayloadLocation.initialize(this, latitude, longitude, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, latitude, longitude, name) { 
        obj['latitude'] = latitude;
        obj['longitude'] = longitude;
        obj['name'] = name;
    }

    /**
     * Constructs a <code>LocationMessagePayloadLocation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:WhatsAPI/models/LocationMessagePayloadLocation} obj Optional instance to populate.
     * @return {module:WhatsAPI/models/LocationMessagePayloadLocation} The populated <code>LocationMessagePayloadLocation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LocationMessagePayloadLocation();

            if (data.hasOwnProperty('address')) {
                obj['address'] = ApiClient.convertToType(data['address'], 'String');
            }
            if (data.hasOwnProperty('latitude')) {
                obj['latitude'] = ApiClient.convertToType(data['latitude'], 'Number');
            }
            if (data.hasOwnProperty('longitude')) {
                obj['longitude'] = ApiClient.convertToType(data['longitude'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LocationMessagePayloadLocation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LocationMessagePayloadLocation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LocationMessagePayloadLocation.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['address'] && !(typeof data['address'] === 'string' || data['address'] instanceof String)) {
            throw new Error("Expected the field `address` to be a primitive type in the JSON string but got " + data['address']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

LocationMessagePayloadLocation.RequiredProperties = ["latitude", "longitude", "name"];

/**
 * @member {String} address
 */
LocationMessagePayloadLocation.prototype['address'] = undefined;

/**
 * @member {Number} latitude
 */
LocationMessagePayloadLocation.prototype['latitude'] = undefined;

/**
 * @member {Number} longitude
 */
LocationMessagePayloadLocation.prototype['longitude'] = undefined;

/**
 * @member {String} name
 */
LocationMessagePayloadLocation.prototype['name'] = undefined;






export default LocationMessagePayloadLocation;

